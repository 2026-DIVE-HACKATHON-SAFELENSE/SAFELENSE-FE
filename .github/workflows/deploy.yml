name: Deploy web to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# 동시에 여러 배포가 겹치지 않도록 직렬화
concurrency:
  group: cf-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    # 시크릿(CLOUDFLARE_API_TOKEN)이 등록되기 전에는 배포 단계를 건너뛰고 빌드만 검증한다.
    env:
      HAS_CF: ${{ secrets.CLOUDFLARE_API_TOKEN != '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build web (expo export -p web → dist)
        run: npm run build:web
        # EXPO_PUBLIC_* 는 빌드 시 번들에 인라인된다. .env 는 CI 에 올라가지 않으므로
        # 레포 Variables 로 주입한다(Settings → Secrets and variables → Actions → Variables).
        # 미설정 시 빈 값이 들어가고, src/config.ts 의 공개 fallback 이 대신 쓰인다.
        env:
          EXPO_PUBLIC_API_BASE_URL: ${{ vars.EXPO_PUBLIC_API_BASE_URL }}
          EXPO_PUBLIC_KAKAO_REST_API_KEY: ${{ vars.EXPO_PUBLIC_KAKAO_REST_API_KEY }}

      # 최초 1회: 프로젝트가 없으면 생성 (이미 있으면 에러 무시)
      - name: Ensure Cloudflare Pages project
        if: ${{ env.HAS_CF == 'true' }}
        continue-on-error: true
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages project create safelense-fe --production-branch=main

      - name: Deploy to Cloudflare Pages
        if: ${{ env.HAS_CF == 'true' }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=safelense-fe --branch=main
